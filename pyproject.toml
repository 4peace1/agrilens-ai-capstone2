[tool.ruff]
line-length = 100
target-version = "py311"
exclude = ["models", "checkpoints", "data"]

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = [
    "E501",  # line length handled loosely for docstrings/comments
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["F401"]
